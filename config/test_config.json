{
  "README": ["This JSON document represents the test configuration options."],

  "source_description": [
    "Connection parameters to your MySQL database",
    "Please ensure, that you have defined all parameters properly.",
    "Ensure, that details like 'charset' are included (if necessary).",
    "Notice, any parameter, acceptable by the node mysql module can be placed here."
  ],
  "source": {
    "host": "127.0.0.1",
    "port": 3306,
    "charset": "utf8mb4",
    "supportBigNumbers": true,
    "database": "nmig_test_db",
    "user": "root",
    "password": "secret"
  },

  "target_description": [
    "Connection parameters to your PostgreSQL database",
    "Please ensure, that you have defined all parameters properly.",
    "Ensure, that details like 'charset' are included (if necessary). nmig_test_db",
    "Notice, any parameter, acceptable by the node pg module can be placed here."
  ],
  "target": {
    "host": "127.0.0.1",
    "port": 5432,
    "charset": "UTF8",
    "database": "postgres",
    "user": "postgres",
    "password": "secret"
  },

  "schema_description": [
    "A name of the schema, that will contain all migrated tables.",
    "If not supplied, then a new schema will be created automatically."
  ],
  "schema": "public",

  "remove_test_resources_description": [
    "If true, then all resources created by test scripts will be removed.",
    "Default - true."
  ],
  "remove_test_resources": true,
  "include_table_like_description": [
    "Only tables, that match this LIKE pattern, will be migrated.",
    "Default - ''."
  ],
  "include_table_like": "hello%",
  "exclude_table_like_description": [
    "Tables, that match this LIKE pattern, will be excluded from migration.",
    "Default - ''."
  ],
  "exclude_table_like": "world%",
  "limit_description": [
    "Limit the number of tables to migrate.",
    "Default - 0 (no limit)."
  ],
  "limit": 1000,
  "offset_description": [
    "Offset the number of tables to migrate.",
    "Default - 0."
  ],
  "offset": 0
}
